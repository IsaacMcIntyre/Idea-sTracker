@model IdeasTracker.Models.UserModel;

@{
    ViewData["Title"] = "Delete Role";
}


@section scripts{
<script>$('#image').css('background-image', 'url(https://i.imgur.com/OjR5Jjd.png)');
    $("#image-text").text("Saying bye to someone?");
    $(window).scroll(function () {
        $(".hero-image").children().css("opacity", 1 - $(window).scrollTop() / 250);
    });</script>
}


<div class="row" id="RoleDelete">

    <div class="col-lg-12">
        <h1 class="mt-3">Delete</h1>
    </div>

    <div class="col-lg-8 mx-auto">
     <h3>Are you sure you want to delete this?</h3>
         
        <p class="role-delete-mini-header">
            @Html.DisplayNameFor(model => model.Name)
        </p>
        <p>
            @Html.DisplayFor(model => model.Name)
        </p>
        <p class="role-delete-mini-header">
            @Html.DisplayNameFor(model => model.Email)
        </p>
        <p>
            @Html.DisplayFor(model => model.Email)
        </p>
        <p class="role-delete-mini-header">
            @Html.DisplayNameFor(model => model.Role)
        </p>
        <p>
            @Html.DisplayFor(model => model.Role)
        </p>
    
    <form asp-action="Delete">
        <input type="hidden" asp-for="Id" />
        <button type="submit" class="btn AND-button"><i class="fas fa-trash-alt"></i> Delete</button>
        <a asp-action="Index" class="btn AND-button"><i class="fas fa-arrow-left"></i> Back to List</a>
    </form>
    </div>
</div>
